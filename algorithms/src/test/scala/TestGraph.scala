import org.scalatest.wordspec.AnyWordSpec
import org.scalatest.matchers.should.Matchers
import algorithms.Graph

class TestGraph extends AnyWordSpec with Matchers {
  "A Graph" should {
    "have a pending test" in {
      pending
    }
  }
}
